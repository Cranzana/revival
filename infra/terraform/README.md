# OmniEdu Terraform Starter

This starter intentionally stays small: it provisions an image repository as the first building block for CI/CD.

Recommended next additions:

- networking and VPC layout
- managed PostgreSQL and Redis
- EKS or ECS application runtime
- secret storage for `DATABASE_URL`, `REDIS_URL`, and `AUTH_SECRET`
